# systemd.altserver.service

## requires

NyaMisty/AltServer-Linux: AltServer for AltStore, but on-device https://github.com/NyaMisty/AltServer-Linux

```

cp AltServer /usr/local/bin/
chmod +x /usr/local/bin/AltServer

```
